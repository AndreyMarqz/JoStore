services:
  postgres:
    image: postgres:18-alpine
    container_name: jostore-postgres
    environment:
      POSTGRES_DB: jostore
      POSTGRES_USER: jostore
      POSTGRES_PASSWORD: jostore_dev
    ports:
      - "5432:5432"
    volumes:
      - jostore_postgres_data:/var/lib/postgresql

volumes:
  jostore_postgres_data: